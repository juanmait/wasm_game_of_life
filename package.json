{
  "name": "rust_web",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "browser-sync --host='rustweb.loc' --port $PORT --server --files=\"src/**/*.mjs,pkg/**/*.js\" --no-open",
    "build": "wasm-pack build --target web --dev",
    "test": "echo \"Error: no test specified\" && exit 1",
    "hotelize": "hotel add 'npm start' --name rustweb"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^6.8.0",
    "browser-sync": "^2.26.7"
  }
}
